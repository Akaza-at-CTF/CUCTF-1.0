# babyrev1

Disassemble the file using objdump -d babyrev1 > babyrev1.asm. Looking at the main function, it compares a string with hardcoded values. Convert these values to get the flag.

The flag is `CUCTF{W3lc0m3_70_R3v3r51ng}`.
